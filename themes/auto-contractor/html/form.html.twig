<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{% if pageTitle is defined %}{{ pageTitle }}{% endif %}</title>
    {% if outputHeadDeclarations is defined %}{{ outputHeadDeclarations() }}{% endif %}
</head>
<body>
<style>
    /* Form Page Styles - Override theme styles for standalone form display */
    body {
        background: #f8fafc !important;
        min-height: 100vh;
        display: flex;
        align-items: center;
        justify-content: center;
        padding: 20px;
    }
    .contractor-theme {
        width: 100%;
        max-width: 500px;
    }
    .form-wrapper {
        background: white;
        border-radius: 12px;
        box-shadow: 0 8px 30px rgba(0,0,0,0.12);
        padding: 40px;
    }
    .form-header {
        text-align: center;
        margin-bottom: 30px;
    }
    .form-header h1 {
        font-size: 28px;
        font-weight: 700;
        color: #1e40af;
        margin-bottom: 10px;
    }
    .form-message {
        background: #dbeafe;
        border-left: 4px solid #1e40af;
        padding: 15px 20px;
        border-radius: 6px;
        margin-bottom: 25px;
    }
    .form-message p {
        color: #1e40af;
        margin: 0;
    }
    .form-message.error {
        background: #fee2e2;
        border-left-color: #dc2626;
    }
    .form-message.error p {
        color: #dc2626;
    }
    .form-message.success {
        background: #d1fae5;
        border-left-color: #059669;
    }
    .form-message.success p {
        color: #059669;
    }
    .mauticform-wrapper {
        max-width: 100%;
    }
    .mauticform-input,
    .mauticform-textarea,
    .mauticform-selectbox {
        width: 100%;
        padding: 12px 16px;
        border: 1px solid #e2e8f0;
        border-radius: 6px;
        font-size: 16px;
        margin-bottom: 15px;
        transition: border-color 0.3s ease, box-shadow 0.3s ease;
    }
    .mauticform-input:focus,
    .mauticform-textarea:focus,
    .mauticform-selectbox:focus {
        outline: none;
        border-color: #1e40af;
        box-shadow: 0 0 0 3px rgba(30, 64, 175, 0.1);
    }
    .mauticform-label {
        display: block;
        font-weight: 500;
        color: #1e293b;
        margin-bottom: 8px;
    }
    .mauticform-button-wrapper .mauticform-button {
        background: #f59e0b;
        color: white;
        padding: 14px 32px;
        font-size: 16px;
        font-weight: 600;
        border: none;
        border-radius: 8px;
        cursor: pointer;
        transition: all 0.3s ease;
        width: 100%;
    }
    .mauticform-button-wrapper .mauticform-button:hover {
        background: #dc8b0c;
        transform: translateY(-2px);
        box-shadow: 0 4px 12px rgba(245, 158, 11, 0.4);
    }
    .mauticform-errormsg {
        color: #dc2626;
        font-size: 14px;
        margin-top: -10px;
        margin-bottom: 10px;
    }
    .mauticform-required {
        color: #dc2626;
    }
    /* Checkbox and radio styling */
    .mauticform-checkbox-grp,
    .mauticform-radiogrp {
        margin-bottom: 15px;
    }
    .mauticform-checkbox-row,
    .mauticform-radio-row {
        display: flex;
        align-items: center;
        margin-bottom: 10px;
    }
    .mauticform-checkbox-row input,
    .mauticform-radio-row input {
        margin-right: 10px;
        width: auto;
    }
    /* Responsive */
    @media (max-width: 600px) {
        .form-wrapper {
            padding: 30px 20px;
        }
        .form-header h1 {
            font-size: 24px;
        }
    }
</style>

<div class="contractor-theme">
    <div class="form-wrapper">
        {# Required: Display message (validation errors, success messages, etc.) #}
        {% if message is defined %}
        <div class="form-message">
            {{ message|raw }}
        </div>
        {% endif %}

        {# Required: Display header #}
        {% if header is defined %}
        <div class="form-header">
            <h1>{{ header }}</h1>
        </div>
        {% endif %}

        {# Required: Display form content #}
        <div class="form-content">
            {% if content is defined %}{{ content|raw }}{% endif %}
        </div>
    </div>
</div>
{% if outputFootDeclarations is defined %}{{ outputFootDeclarations() }}{% endif %}
</body>
</html>
